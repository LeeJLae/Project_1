for result in results:
    if result is not None:
        obb_boxes = result.obb.xyxy  # For OBB models
        for obb_box in obb_boxes:
            print(f"OBB Box: {obb_box}")
    else:
        print("No detections.")
